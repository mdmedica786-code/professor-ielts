fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
# NOTE: torch and torchaudio are installed separately in the Dockerfile from
# the CPU-only index (https://download.pytorch.org/whl/cpu). Do NOT add them
# back here, or pip will pull the multi-GB CUDA build from PyPI.
transformers>=4.40.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
textgrid>=1.6
g2p-en>=2.1.0
phonemizer